<%= form_for todo do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>

  <%= f.label :time %>
  <%= f.datetime_field :time %><br>

  <%= f.collection_select :category_id, Category.all, :id, :name %><br>

  <%= f.fields_for :category, @todo.build_category do |category_form| %>
    <%= category_form.label :name, "Category" %>
    <%= category_form.text_field :name %><br>
  <% end %>

  <br>
  <%= f.submit %>
<% end %> 
  
    
  
     
  

